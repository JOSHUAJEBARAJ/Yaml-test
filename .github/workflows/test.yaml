name: sca

on:
  push:

jobs:
  checkout-repo:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v2
    - name: Kubescape custom actionss
      uses: JOSHUAJEBARAJ/Kubescape-custom-actions@0.1

